{
    "minimum-stability": "stable",
    "prefer-stable": true,
    "config": {
        "platform": {
            "php": "7.0"
        },
        "vendor-dir": "/opt/repositories/php-env/vendor"
    },
    "require": {
        "phpunit/phpunit": "^6.5"
    }
}
